#include <stdio.h>
#include <math.h>
int main()
{
    int A, B, sum;
    printf(" enter a number A \n");
    scanf("%d", &A);
    printf("enter a number B \n");
    scanf("%d", &B);
    sum = (A+B);
    printf("the value of sum is=%d", sum);
    return 0;
}
